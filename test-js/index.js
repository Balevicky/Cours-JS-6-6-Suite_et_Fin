// Installer "parcel" il faut taper dans le terminal de vscode:"npm install --save-dev parcel"
// pour lancer parcel ilfaut:npx parcel index.html
// " installer "jest": il faut taper dans le terminal de vscode: "npm install --save-dev jest"

import { addNum } from "./add";
console.log(addNum(5, 10));
